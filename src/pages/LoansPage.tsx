import { useCallback,useEffect,useState } from 'react'; import axios from 'axios'; import { adminApi } from '../api'; import { ErrorBanner,PageHeader,RemoteTable,secondaryButtonClass } from '../components/RemoteTable'; type Loan={_id:string;driverId:unknown;loanAmount:number;loanStatus:string;remainingBalance:number;createdAt:string}; export function LoansPage(){const[rows,setRows]=useState<Loan[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=useCallback(async()=>{setLoading(true);try{const r=await adminApi.loans();setRows(r.data.data||[]);setError('');}catch(e){setError(axios.isAxiosError(e)?String(e.response?.data?.message||e.message):'Unexpected error');}finally{setLoading(false);}},[]);useEffect(()=>{void load();},[load]);const decide=async(id:string,action:'approve'|'reject')=>{if(!window.confirm(`${action} this loan?`))return;try{await adminApi.decideLoan(id,action);await load();}catch(e){setError(axios.isAxiosError(e)?String(e.response?.data?.message||e.message):'Unexpected error');}};return <section><PageHeader title="Loans" description="Review and decide real loan applications."/>{error?<ErrorBanner message={error} retry={load}/>:null}<RemoteTable heads={['Loan','Driver','Amount','Balance','Status','Created','Actions']} loading={loading} empty={!rows.length}>{rows.map(r=><tr key={r._id} className="border-t border-white/5"><td className="py-4 pr-4">{r._id.slice(-8)}</td><td className="py-4 pr-4">{typeof r.driverId==='object'?JSON.stringify(r.driverId):String(r.driverId)}</td><td className="py-4 pr-4">{r.loanAmount}</td><td className="py-4 pr-4">{r.remainingBalance}</td><td className="py-4 pr-4">{r.loanStatus}</td><td className="py-4 pr-4">{new Date(r.createdAt).toLocaleDateString()}</td><td>{r.loanStatus==='pending'?<div className="flex gap-2"><button className={secondaryButtonClass} onClick={()=>decide(r._id,'approve')}>Approve</button><button className={secondaryButtonClass} onClick={()=>decide(r._id,'reject')}>Reject</button></div>:'—'}</td></tr>)}</RemoteTable></section>}
