import { adminApi } from '../api'; import { LiveListPage } from '../components/LiveListPage'; const load=()=>adminApi.loans(); export function LoansPage(){return <LiveListPage title="Loans" description="All loan records returned by the protected admin loan endpoint." load={load} columns={[{key:'_id',label:'Loan'},{key:'driverId',label:'Driver'},{key:'amount',label:'Amount'},{key:'status',label:'Status'},{key:'outstandingBalance',label:'Outstanding'},{key:'createdAt',label:'Created'}]}/>}
