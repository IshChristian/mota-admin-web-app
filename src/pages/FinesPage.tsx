import { adminApi } from '../api'; import { LiveListPage } from '../components/LiveListPage'; const load=()=>adminApi.fines(); export function FinesPage(){return <LiveListPage title="Pending fines" description="Real fine requests awaiting authorized review." load={load} columns={[{key:'fineId',label:'Fine'},{key:'driverId',label:'Driver'},{key:'amount',label:'Amount'},{key:'reason',label:'Reason'},{key:'status',label:'Status'},{key:'createdAt',label:'Created'}]}/>}
