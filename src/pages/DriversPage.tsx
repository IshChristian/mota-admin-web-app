import { adminApi } from '../api'; import { LiveListPage } from '../components/LiveListPage'; const load=()=>adminApi.drivers({limit:100}); export function DriversPage(){return <LiveListPage title="Drivers" description="Live driver accounts, approval state, availability and last GPS update." load={load} columns={[{key:'firstName',label:'Driver',render:r=>`${r.firstName||''} ${r.lastName||''}`.trim()},{key:'phone',label:'Phone'},{key:'registrationStatus',label:'Registration'},{key:'isOnline',label:'Online'},{key:'lastLocationAt',label:'Last GPS'},{key:'isActive',label:'Active'}]}/>}
