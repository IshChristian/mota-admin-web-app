import { adminApi } from '../api'; import { LiveListPage } from '../components/LiveListPage'; const load=()=>adminApi.audits({limit:100}); export function AuditPage(){return <LiveListPage title="Audit trail" description="Immutable staff actions recorded by the backend." load={load} columns={[{key:'actorId',label:'Actor'},{key:'action',label:'Action'},{key:'targetType',label:'Target type'},{key:'targetId',label:'Target'},{key:'ipAddress',label:'IP address'},{key:'timestamp',label:'Time'}]}/>}
