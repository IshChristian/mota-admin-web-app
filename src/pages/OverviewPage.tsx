import { useCallback, useEffect, useState } from 'react'; import axios from 'axios'; import { adminApi } from '../api'; import { ErrorBanner, PageHeader } from '../components/RemoteTable';
export function OverviewPage(){const[data,setData]=useState<Record<string,unknown>>({});const[loading,setLoading]=useState(true);const[error,setError]=useState('');const load=useCallback(async()=>{setLoading(true);setError('');try{const r=await adminApi.stats();setData(r.data.data||r.data||{});}catch(e){setError(axios.isAxiosError(e)?String(e.response?.data?.message||e.message):'Unexpected error');}finally{setLoading(false);}},[]);useEffect(()=>{void load();},[load]);return <section><PageHeader title="Operations overview" description="Live platform statistics returned by the production API."/>{error?<ErrorBanner message={error} retry={load}/>:null}{loading?<p className="text-slate-400">Loading live statistics…</p>:<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{Object.entries(data).map(([key,value])=><div key={key} className="rounded-2xl border border-white/10 bg-panel p-5"><div className="text-sm capitalize text-slate-400">{key.replace(/([A-Z_])/g,' $1')}</div><div className="mt-5 break-words text-2xl font-semibold">{typeof value==='object'?JSON.stringify(value):String(value)}</div></div>)}{!Object.keys(data).length?<p className="text-slate-400">No statistics returned.</p>:null}</div>}</section>}
