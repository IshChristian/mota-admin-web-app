import { adminApi } from '../api'; import { LiveListPage } from '../components/LiveListPage'; const load=()=>adminApi.transactions({limit:100}); export function FinancePage(){return <LiveListPage title="Transactions" description="Paypack-backed transaction records. Synchronization is executed by the backend and audited." load={load} action={{label:'Sync pending',run:()=>adminApi.syncTransactions()}} columns={[{key:'ref',label:'Reference'},{key:'client',label:'Client'},{key:'kind',label:'Type'},{key:'amount',label:'Amount'},{key:'status',label:'Status'},{key:'created_at',label:'Created'}]}/>}
