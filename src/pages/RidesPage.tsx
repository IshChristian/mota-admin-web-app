import { adminApi } from '../api'; import { LiveListPage } from '../components/LiveListPage'; const load=()=>adminApi.rides({limit:100}); export function RidesPage(){return <LiveListPage title="Rides" description="Live ride lifecycle records. Operational actions remain protected by the ride API." load={load} columns={[{key:'_id',label:'Ride'},{key:'passengerId',label:'Passenger'},{key:'driverId',label:'Driver'},{key:'status',label:'Status'},{key:'fare',label:'Fare'},{key:'createdAt',label:'Created'}]}/>}
