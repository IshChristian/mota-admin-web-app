import{BrowserRouter,Navigate,Route,Routes,useLocation}from'react-router-dom';import{AuthProvider,useAuth}from'./auth';import{Shell}from'./components/Shell';import{AuditPage,FinancePage,OverviewPage,PeoplePage,RegistrationsPage,RidesPage,RolesPage,SettingsPage}from'./pages';import{LoginPage}from'./pages/LoginPage';
function Protected(){const{staff}=useAuth();const l=useLocation();return staff?<Shell/>:<Navigate to="/login" state={{from:l.pathname}} replace/>}
export default function App(){return <BrowserRouter><AuthProvider><Routes><Route path="login" element={<LoginPage/>}/><Route element={<Protected/>}><Route index element={<Navigate to="/overview" replace/>}/><Route path="overview" element={<OverviewPage/>}/><Route path="people" element={<PeoplePage/>}/><Route path="rides" element={<RidesPage/>}/><Route path="finance" element={<FinancePage/>}/><Route path="registrations" element={<RegistrationsPage/>}/><Route path="roles" element={<RolesPage/>}/><Route path="audit" element={<AuditPage/>}/><Route path="settings" element={<SettingsPage/>}/><Route path="*" element={<Navigate to="/overview" replace/>}/></Route></Routes></AuthProvider></BrowserRouter>}
