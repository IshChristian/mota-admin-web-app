import{useEffect,useState}from'react';import{Link}from'react-router-dom';const KEY='mota_admin_privacy_v1';export function CookieConsent(){const[open,setOpen]=useState(false);useEffect(()=>setOpen(!localStorage.getItem(KEY)),[]);if(!open)return null;const save=(v:string)=>{localStorage.setItem(KEY,v);setOpen(false)};return <aside className="fixed bottom-4 left-4 right-4 z-[100] mx-auto max-w-3xl rounded-2xl border border-white/10 bg-slate-950 p-5 text-slate-100 shadow-2xl"><b>Cookie and privacy preferences</b><p className="mt-2 text-sm text-slate-400">Essential storage keeps staff sessions secure. Optional analytics require consent.</p><div className="mt-4 flex flex-wrap gap-3"><button onClick={()=>save('essential')} className="rounded-xl border border-white/10 px-4 py-2">Essential only</button><button onClick={()=>save('all')} className="rounded-xl bg-lime px-4 py-2 font-bold text-ink">Accept all</button><Link to="/info/privacy" className="px-3 py-2 text-lime">Privacy policy</Link></div></aside>}
