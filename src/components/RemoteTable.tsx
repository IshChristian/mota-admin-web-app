import type { ReactNode } from 'react';
export function PageHeader({ title, description, action }: { title: string; description: string; action?: ReactNode }) { return <header className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><h2 className="text-2xl font-semibold">{title}</h2><p className="mt-1 text-sm text-slate-400">{description}</p></div>{action}</header>; }
export function RemoteTable({ heads, children, empty, loading }: { heads: string[]; children: ReactNode; empty: boolean; loading: boolean }) { return <div className="overflow-x-auto rounded-2xl border border-white/10 bg-panel p-5"><table className="w-full min-w-[760px] text-left text-sm"><thead className="text-xs uppercase text-slate-500"><tr>{heads.map((head) => <th key={head} className="pb-4 pr-4">{head}</th>)}</tr></thead><tbody>{children}</tbody></table>{loading ? <p className="py-10 text-center text-slate-400">Loading live data…</p> : null}{!loading && empty ? <p className="py-10 text-center text-slate-400">No records found.</p> : null}</div>; }
export function ErrorBanner({ message, retry }: { message: string; retry: () => void }) { return <div role="alert" className="mb-5 flex items-center justify-between rounded-xl border border-red-400/30 bg-red-400/10 p-4 text-sm text-red-200"><span>{message}</span><button onClick={retry} className="rounded-lg border border-red-300/30 px-3 py-1.5">Retry</button></div>; }
export const buttonClass = 'rounded-xl bg-lime px-4 py-2 text-sm font-bold text-ink disabled:cursor-not-allowed disabled:opacity-50';
export const secondaryButtonClass = 'rounded-xl border border-white/10 px-3 py-2 text-sm hover:bg-white/5 disabled:opacity-50';
export const inputClass = 'w-full rounded-xl border border-white/10 bg-white/5 px-3 py-2.5 text-sm outline-none focus:border-lime';
