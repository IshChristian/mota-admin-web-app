import{useState}from'react';import{Activity,Bell,Car,CircleDollarSign,FileWarning,Landmark,LayoutDashboard,LogOut,Map,Menu,Settings,ShieldCheck,UserCircle,Users,X}from'lucide-react';import{NavLink,Outlet,useLocation}from'react-router-dom';import{useAuth}from'../auth';
const items=[['/overview','Overview',LayoutDashboard,'admin:access'],['/users','Users',Users,'user:view'],['/drivers','Drivers',Car,'driver:view'],['/rides','Rides',Car,'ride:view'],['/finance','Transactions',CircleDollarSign,'finance:view'],['/loans','Loans',Landmark,'loan:view'],['/fines','Fines',FileWarning,'fines:view'],['/registrations','Registrations',ShieldCheck,'registration:view'],['/roles','Staff & roles',Users,'role:view'],['/audit','Audit trail',Activity,'audit:view'],['/support','Caller support',Users,'support:view'],['/operations-map','Live map',Map,'support:view'],['/settings','Settings',Settings,'settings:view']]as const;
export function Shell(){const[open,setOpen]=useState(false);const location=useLocation();const{staff,can,logout}=useAuth();const visible=items.filter(item=>can(item[3]));const title=items.find(item=>location.pathname.startsWith(item[0]))?.[1]||(location.pathname==='/profile'?'My profile':'MOTA Control');return <div className="min-h-screen text-slate-100">{open?<button aria-label="Close menu" className="fixed inset-0 z-30 bg-black/60 lg:hidden" onClick={()=>setOpen(false)}/>:null}<aside className={`fixed inset-y-0 left-0 z-40 flex w-72 flex-col border-r border-white/10 bg-ink/95 p-5 backdrop-blur-xl transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="mb-5 flex shrink-0 items-center justify-between"><NavLink to="/overview" className="flex items-center gap-3"><img src="/mota-logo.png" alt="MOTA" className="h-12 w-20 object-contain"/><span><b>MOTA CONTROL</b><small className="block text-xs text-slate-500">Operations console</small></span></NavLink><button aria-label="Close navigation" className="lg:hidden" onClick={()=>setOpen(false)}><X/></button></div><nav className="min-h-0 flex-1 space-y-1 overflow-y-auto pr-1">{visible.map(([path,label,Icon])=><NavLink key={path} to={path} onClick={()=>setOpen(false)} className={({isActive})=>`flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-medium ${isActive?'bg-lime text-ink':'text-slate-400 hover:bg-white/5 hover:text-white'}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="mt-4 shrink-0 rounded-2xl border border-white/10 bg-white/5 p-4"><NavLink to="/profile" onClick={()=>setOpen(false)} className="flex items-center gap-3"><UserCircle size={28}/><span className="min-w-0"><b className="block truncate text-sm">{staff?.name}</b><small className="capitalize text-slate-500">{staff?.role.replace('_',' ')}</small></span></NavLink><button onClick={logout} className="mt-3 flex items-center gap-2 text-xs text-slate-400 hover:text-white"><LogOut size={14}/>Log out</button></div></aside><main className="lg:pl-72"><header className="sticky top-0 z-20 flex h-20 items-center gap-4 border-b border-white/10 bg-ink/80 px-5 backdrop-blur-xl md:px-8"><button aria-label="Open navigation" className="lg:hidden" onClick={()=>setOpen(true)}><Menu/></button><div><h1 className="text-xl font-semibold">{title}</h1><p className="hidden text-xs text-slate-500 sm:block">Live operations and access control</p></div><button aria-label="Notifications" className="ml-auto rounded-xl border border-white/10 p-2.5"><Bell size={18}/></button></header><div className="p-5 md:p-8"><Outlet/></div></main></div>}
